[package]
name = "mouser-state"
version = "0.0.0"
description = "Mouser cluster state: the spec §7.2 shared-config CRDT (automerge, fmt=1)."
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
automerge = "0.5"

[lints]
workspace = true
